// Tipos de dados primitivos
// String, Number, Undefined, Null, Boolean, Symbol (Ainda não foi explicado sobre)
const nome = 'Lucas'; // String
const nome1 = "Lucas"; // String
const nome2 = `Lucas`; // String
const num1 = 10; // Number
const num2 = 10.52; // Number
let nomeAluno; // Undefined -> não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> não aponta para local nenhum na memória | deve ser usada quando queremos zerar o valor de uma variável
const aprovado = true; // Boolean -> true ou false (lógico)
